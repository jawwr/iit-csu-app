class User {
  int id;
  String name, surname, groupName, email, password;

  User(
      {required this.id,
      required this.name,
      required this.surname,
      required this.groupName,
      required this.email,
      required this.password});
}
